{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug tests",
      "program": "${workspaceFolder}/zig-out/bin/debug-unit-tests",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zig: build tests"
    }
  ]
}
